<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
       <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryDark">@color/primaryDarkColor</item>
        <item name="colorSecondary">@color/primaryLightColor</item>
        <item name="colorAccent">@color/primaryTextColor</item>
        <item name="fontFamily">@font/copse</item>
        <item name="android:fontFamily">@font/copse</item>
        <item name="textAppearanceHeadline6">@style/CustomHeadline6</item>
    </style>

    <style name="CustomHeadline6" parent="TextAppearance.MaterialComponents.Headline6">CustomHeadline6
        <item name="android:textSize">18sp</item>
    </style>

</resources>